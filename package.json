{
  "name": "sharepoint-office-launcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs config/targets.json enterprise",
    "build:enterprise": "node scripts/build.mjs config/targets.json enterprise",
    "build:dev": "node scripts/build.mjs config/targets.json dev",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
